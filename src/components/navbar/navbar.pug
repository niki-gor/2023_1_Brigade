header.header.flex
    .header__img.flex
        img(src="./assets/img/logo.png", alt="Our logo").header__logo
        != UserImage
    nav.nav.header__nav.flex
        ul.nav__list.flex
            each item in NavList
                != item
