.message(class=className, style=style)
    if (avatar != '')
        .message__avatar
    if (nickname != '')
        span.message__nickname= nickname
    if (image != '')
        .message__image
    if (body != '')
        .message__body= body.replace(/(.{50})/g, '$1<br>')
